local tl = ...---@type MainLibObject
local DisplayText = tl.baseClass:new()---@class MonitorDefinition:BaseClass

local type, tonumber, error, sub = type, tonumber, error, string.sub

---@protected
---@param option {text:string, maxLines:number}
function DisplayText:constructor(option)

end

function DisplayText:reset()

end

function DisplayText:currentPage()
  
end

function DisplayText:nextPage()
  
end

---@param num number
function DisplayText:toPage(num)
  
end

return DisplayText